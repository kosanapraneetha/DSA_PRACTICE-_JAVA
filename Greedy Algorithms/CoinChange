//Problem:Selecting minimum  number of coins for an amount in given denominations.
//Approach:Greedy approach 
import java.util.Arrays;
    import java.util.Collections;
    public class CoinChange{
        public static int coins(int[] Coins,int amount){
            if(amount<=0) return 0;
            java.util.Arrays.sort(Coins);//by default Ascending order
            int n=arrCoins.length;
            int count=0;
            for(int i=n-1;i>=0;i--){
                int coin=Coins[i];
                int use=amount/coin;
                count+=use;
                amount-=coin*use;
            }
            return count;
        }
        public static void main(String[] args){
            int[] Coins={5,1,25,10};
            System.out.println(coins(Coins,41));
        }
    }
//Time Complexity:O(n logn) --due to sorting
//Space Complexity:O(1)
